# Helpers Rules

## When to create a helper
- Explicitly asked by user 
- Recurrent action that takes time (test protocol, run code with particular config...)

## How to create a helper
1. Create script (python, bash...) and necessary files in `codex/helpers/helper_name`.
2. List helper in `codex/memory/HELPERS.md` with short explanation and example uses.